{{ ansible_managed | comment }}
# Read by schema-registry.service through EnvironmentFile.

JAVA_HOME={{ schema_registry_java_home }}
SCHEMA_REGISTRY_HEAP_OPTS=-Xms{{ schema_registry_heap_size }} -Xmx{{ schema_registry_heap_size }}

# Confluent's start scripts read this to find the distribution.
CONFLUENT_HOME={{ schema_registry_install_dir }}
LOG_DIR={{ schema_registry_log_dir }}
