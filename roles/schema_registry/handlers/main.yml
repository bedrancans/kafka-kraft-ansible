---
- name: Restart schema-registry
  ansible.builtin.systemd_service:
    name: "{{ schema_registry_service_name }}"
    state: restarted
    daemon_reload: true
  when: schema_registry_service_installed | default(false)
