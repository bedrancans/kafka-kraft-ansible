---
- name: Restart kafka-ui
  ansible.builtin.systemd_service:
    name: "{{ kafka_ui_service_name }}"
    state: restarted
    daemon_reload: true
  when: kafka_ui_service_installed | default(false)
