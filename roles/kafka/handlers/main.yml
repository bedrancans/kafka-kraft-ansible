---
# Handlers run once, at the end of the play, and only if something notified
# them. Five changed config files therefore cause one restart, not five.
- name: Restart kafka
  ansible.builtin.systemd_service:
    name: "{{ kafka_service_name }}"
    state: restarted
    daemon_reload: true
  when: kafka_service_installed | default(false)
