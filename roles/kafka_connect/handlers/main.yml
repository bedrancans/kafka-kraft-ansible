---
- name: Restart kafka-connect
  ansible.builtin.systemd_service:
    name: "{{ kafka_connect_service_name }}"
    state: restarted
    daemon_reload: true
  when: kafka_connect_service_installed | default(false)
