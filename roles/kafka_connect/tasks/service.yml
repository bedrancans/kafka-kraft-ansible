---
- name: Install the systemd unit
  ansible.builtin.template:
    src: kafka-connect.service.j2
    dest: "/etc/systemd/system/{{ kafka_connect_service_name }}.service"
    owner: root
    group: root
    mode: "0644"
  notify: Restart kafka-connect

- name: Mark the service as installed
  ansible.builtin.set_fact:
    kafka_connect_service_installed: true

- name: Enable and start Kafka Connect
  ansible.builtin.systemd_service:
    name: "{{ kafka_connect_service_name }}"
    enabled: true
    state: started
    daemon_reload: true
