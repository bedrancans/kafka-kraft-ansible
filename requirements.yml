---
# Collections this repository depends on.
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix      # sysctl, mount, authorized_key
    version: ">=1.5.4"
  - name: community.general  # java_keystore (phase 8), misc helpers
    version: ">=8.0.0"
  - name: containers.podman  # molecule's podman driver connection plugin
    version: ">=1.13.0"
